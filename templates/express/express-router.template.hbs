import * as Soap from '@soapjs/soap';
import { Config } from './config';
{{~#each imports~}}
{{{this}}}
{{~/each~}}

export class Router extends SoapExpressRouter {
  constructor({{{params}}})
  public configure() {

  }
}
